addons:
  artifacts:
    debug: true
    paths:
    - libs.zip
    - build.zip
    - testbuild.zip
    target_paths: 4d31348c-533d-455f-98f9-6a1f27914042
after_success:
- ./df-build.sh
before_install:
- sed -i.bak -e 's|https://nexus.codehaus.org/snapshots/|https://oss.sonatype.org/content/repositories/codehaus-snapshots/|g'
  $HOME/.m2/settings.xml
before_script:
- rm -rf $HOME/.m2/repository/org/apache/accumulo
- echo "<settings xmlns='http://maven.apache.org/SETTINGS/1.0.0'><mirrors><mirror><id>secure-central</id><url>https://repo.maven.apache.org/maven2</url><mirrorOf>central</mirrorOf></mirror></mirrors></settings>"
  > $HOME/settings-custom.xml
cache:
  directories:
  - $HOME/.m2
  - $HOME/.m2
install: true
jdk:
- oraclejdk8
language: java
script: mvn --settings $HOME/settings-custom.xml clean verify -DskipITs
# Modified by Insights Service at 2016-06-15 09:55:46.065789
