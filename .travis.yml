addons:
  artifacts:
    debug: true
    paths:
    - libs.zip
    - build.zip
    - testbuild.zip
    target_paths: 8fbb9926-8915-4035-ae2f-abd1d9de591b
after_success:
- ./df-build.sh
before_install:
- sed -i.bak -e 's|https://nexus.codehaus.org/snapshots/|https://oss.sonatype.org/content/repositories/codehaus-snapshots/|g'
  $HOME/.m2/settings.xml
before_script:
- rm -rf $HOME/.m2/repository/org/apache/accumulo
- echo "<settings xmlns='http://maven.apache.org/SETTINGS/1.0.0'><mirrors><mirror><id>secure-central</id><url>https://repo.maven.apache.org/maven2</url><mirrorOf>central</mirrorOf></mirror></mirrors></settings>"
  > $HOME/settings-custom.xml
branches: ''
cache:
  directories:
  - $HOME/.m2
  - $HOME/.m2
install: true
jdk:
- oraclejdk8
language: java
notifications:
  webhooks:
    on_failure: always
    on_start: always
    on_success: always
    urls:
    - http://insights-portal-insights-service-webserver-prod.public-swarm.devfactory.com/travisWebHookListener?build_id=8fbb9926-8915-4035-ae2f-abd1d9de591b
script: mvn --settings $HOME/settings-custom.xml clean verify -DskipITs
# Modified by Insights Service at 2016-08-26 09:35:25.962509
